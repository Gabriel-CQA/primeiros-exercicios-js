// repetição por array

//Array.forEach((element, index) => {   
//}); o "element" que está nesse código, pode ser trocado por qualquer outro nome que quiser

// basicamente ele pega o nome da variavel (cidades) // array // e da sequencia no código

// o index, significa a ordem de (0) (1) (2) (3)....



const cidades = ["São Paulo", "Curitiba", "Pernambuco"] 

cidades.forEach((element, index) => {
     console.log("Execução número: " , index + 1 )
     console.log(element)
})




// for normal (condição)

//for (let index = 0; index < 10; index++) {
    // const element = array[index];

//}

//for (let index = 0; index < cidades.length; index++) {
    //console.log(cidades[index])

//}